DB_USER=postgres
DB_HOST=localhost
DB_NAME=eflow_db_v2
DB_PASSWORD=password123
DB_PORT=5432

PORT=5000
NODE_ENV=production

JWT_SECRET=super_secret_jwt_key_for_local_testing
APPROVAL_SIGNING_SECRET=super_secret_signing_key_for_local_testing

# Dummy SMTP configuration since we are running locally (nodemailer will print OTPs to console in dev mode, or we can use fake credentials)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test
SMTP_PASS=test
FROM_EMAIL=no-reply@eflow-system.org
